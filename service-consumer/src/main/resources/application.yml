server:
  port: 8889
spring:
  application:
    name: service-consumer
eureka:
  client:
    register-with-eureka: false #表示是否向注册中心注册自己
    service-url:
      defaultZone: http://localhost:7001/eureka/